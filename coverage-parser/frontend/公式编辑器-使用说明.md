# 💰 公式编辑器组件 - 使用说明

**版本**: v1.0  
**创建时间**: 2026-01-09  
**组件路径**: `src/components/FormulaEditor.tsx`

---

## 📋 目录

1. [功能概述](#功能概述)
2. [设计理念](#设计理念)
3. [快速开始](#快速开始)
4. [API 文档](#api-文档)
5. [公式类型说明](#公式类型说明)
6. [使用示例](#使用示例)
7. [集成指南](#集成指南)
8. [常见问题](#常见问题)

---

## 功能概述

`FormulaEditor` 是一个用于编辑和计算保险赔付金额的React组件，支持：

✅ **7种常见公式类型**：基本保额、有效保额、Max函数、Min函数、固定金额等  
✅ **两层交互设计**：先选公式类型，再编辑具体变量值  
✅ **实时计算**：变量修改后立即更新计算结果  
✅ **视觉提示**：鼠标悬停显示可编辑状态（下划线 + 铅笔图标）  
✅ **数字格式化**：自动添加千分位分隔符  
✅ **类型安全**：TypeScript + 输入校验  

---

## 设计理念

### 🎯 两层交互模型

#### 第一层：公式类型选择
用户通过下拉菜单选择公式的**整体结构**：
- `基本保额 × N%`
- `Max(基本保额, 已交保费, 现金价值)`
- `Min(约定金额, 基本保额 × N%)`
- 等...

#### 第二层：变量值编辑
选择公式类型后，显示该公式包含的所有变量，用户可以**点击编辑**每个变量的具体数值：
- `基本保额 = 500,000 元`
- `赔付比例 = 150%`

### 🎨 核心设计原则

| 原则 | 说明 | 实现方式 |
|------|------|----------|
| **明确可编辑性** | 用户一眼就知道哪些值可以修改 | 悬停显示铅笔图标 + 边框高亮 |
| **所见即所得** | 显示格式化后的公式和结果 | `基本保额 500,000 × 150% = 750,000 元` |
| **逻辑清晰** | 两层结构符合心智模型 | 先选"做什么"，再定"怎么做" |
| **实时反馈** | 修改立即生效 | 使用 React Hooks 实时计算 |
| **防错设计** | 只允许合法输入 | 正则校验 + 类型检查 |

---

## 快速开始

### 1. 访问演示页面

启动前端服务后，访问：
```
http://localhost:5173/formula-demo
```

### 2. 基础用法

```tsx
import FormulaEditor from '@/components/FormulaEditor'

function MyComponent() {
  return (
    <FormulaEditor
      initialType="basic_percentage"
      initialVariables={{
        basicAmount: 500000,
        percentage: 150
      }}
      onChange={(type, variables, result) => {
        console.log('公式变化:', { type, variables, result })
      }}
    />
  )
}
```

---

## API 文档

### Props

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `initialType` | `FormulaType` | `'basic_percentage'` | 初始公式类型 |
| `initialVariables` | `Record<string, number>` | `undefined` | 初始变量值 |
| `onChange` | `Function` | `undefined` | 值变化回调 |
| `showResult` | `boolean` | `true` | 是否显示计算结果 |

### FormulaType（公式类型）

```typescript
type FormulaType = 
  | 'basic_percentage'        // 基本保额 × N%
  | 'effective_percentage'    // 有效保额 × N%
  | 'max_function'            // Max(基本保额, 已交保费, 现金价值)
  | 'min_function'            // Min(约定金额, 基本保额 × N%)
  | 'fixed_amount'            // 固定金额
  | 'premium_only'            // 已交保费
  | 'cash_value_only'         // 现金价值
```

### onChange 回调参数

```typescript
onChange(
  type: FormulaType,           // 当前公式类型
  variables: Record<string, number>,  // 当前变量值
  result: number               // 计算结果
): void
```

**示例**：
```typescript
onChange: (type, variables, result) => {
  // type: 'basic_percentage'
  // variables: { basicAmount: 500000, percentage: 150 }
  // result: 750000
}
```

---

## 公式类型说明

### 1. 基本保额 × N% (`basic_percentage`)

**公式**：`基本保额 × 赔付比例`

**变量**：
- `basicAmount` - 基本保额（元）
- `percentage` - 赔付比例（%）

**计算**：`result = basicAmount × percentage / 100`

**使用场景**：
- 重大疾病保险：等待期后确诊，按基本保额的 100% 赔付
- 轻症疾病保险：按基本保额的 30% 赔付
- 特定疾病额外给付：按基本保额的 50% 额外赔付

---

### 2. 有效保额 × N% (`effective_percentage`)

**公式**：`有效保额 × 赔付比例`

**变量**：
- `effectiveAmount` - 有效保额（元）
- `percentage` - 赔付比例（%）

**计算**：`result = effectiveAmount × percentage / 100`

**使用场景**：
- 带增长率的保单：有效保额 = 基本保额 × (1 + 增长率)^年数

---

### 3. Max 函数 (`max_function`)

**公式**：`Max(基本保额, 已交保费, 现金价值)`

**变量**：
- `basicAmount` - 基本保额（元）
- `premiumPaid` - 已交保费（元）
- `cashValue` - 现金价值（元）

**计算**：`result = Math.max(basicAmount, premiumPaid, cashValue)`

**使用场景**：
- 等待期内确诊：按基本保额、已交保费、现金价值的较大者赔付
- 身故责任：取多个值中的最大值

---

### 4. Min 函数 (`min_function`)

**公式**：`Min(约定金额, 基本保额 × N%)`

**变量**：
- `agreedAmount` - 约定金额（元）
- `basicAmount` - 基本保额（元）
- `percentage` - 比例上限（%）

**计算**：`result = Math.min(agreedAmount, basicAmount × percentage / 100)`

**使用场景**：
- 上限约束：按约定金额赔付，但最高不超过基本保额的 30%
- 限额赔付：取约定金额和保额比例中的较小值

---

### 5. 固定金额 (`fixed_amount`)

**公式**：`固定金额`

**变量**：
- `fixedAmount` - 固定金额（元）

**计算**：`result = fixedAmount`

**使用场景**：
- 固定赔付金额（如 100,000 元）

---

### 6. 已交保费 (`premium_only`)

**公式**：`已交保费`

**变量**：
- `premiumPaid` - 已交保费（元）

**计算**：`result = premiumPaid`

**使用场景**：
- 等待期内非意外确诊：退还已交保费

---

### 7. 现金价值 (`cash_value_only`)

**公式**：`现金价值`

**变量**：
- `cashValue` - 现金价值（元）

**计算**：`result = cashValue`

**使用场景**：
- 退保：返还现金价值

---

## 使用示例

### 示例1：集成到责任编辑表单

```tsx
import { Form } from 'antd'
import FormulaEditor from '@/components/FormulaEditor'

function CoverageEditForm() {
  const [form] = Form.useForm()
  
  return (
    <Form form={form}>
      <Form.Item label="责任名称" name="name">
        <Input />
      </Form.Item>
      
      <Form.Item label="赔付金额公式" name="formula">
        <FormulaEditor
          initialType="basic_percentage"
          onChange={(type, variables, result) => {
            // 将公式信息保存到表单
            form.setFieldsValue({
              formula: {
                type,
                variables,
                result
              }
            })
          }}
        />
      </Form.Item>
    </Form>
  )
}
```

### 示例2：只显示公式，不显示结果

```tsx
<FormulaEditor
  initialType="basic_percentage"
  showResult={false}  // 隐藏计算结果
/>
```

### 示例3：从JSON数据初始化

```tsx
const savedFormula = {
  type: 'max_function',
  variables: {
    basicAmount: 500000,
    premiumPaid: 120000,
    cashValue: 80000
  }
}

<FormulaEditor
  initialType={savedFormula.type}
  initialVariables={savedFormula.variables}
  onChange={(type, variables, result) => {
    // 保存到数据库或状态管理
    saveFormula({ type, variables, result })
  }}
/>
```

### 示例4：响应式布局

```tsx
import { Row, Col } from 'antd'

<Row gutter={16}>
  <Col xs={24} md={12}>
    <FormulaEditor
      initialType="basic_percentage"
    />
  </Col>
  <Col xs={24} md={12}>
    {/* 其他内容 */}
  </Col>
</Row>
```

---

## 集成指南

### 1. 在 CoverageList 中集成

更新 `CoverageList.tsx`，添加公式编辑功能：

```tsx
import FormulaEditor from './FormulaEditor'

// 在 Card 中添加公式编辑区域
<Card>
  {/* ... 其他内容 ... */}
  
  <div style={{ marginTop: 16 }}>
    <FormulaEditor
      initialType={coverage.formula?.type}
      initialVariables={coverage.formula?.variables}
      onChange={(type, variables, result) => {
        // 更新 coverage 数据
        onUpdateFormula(coverage.id, { type, variables, result })
      }}
    />
  </div>
</Card>
```

### 2. 在后端API中保存公式

```typescript
// 前端发送
POST /api/coverages/:id/formula
{
  "type": "basic_percentage",
  "variables": {
    "basicAmount": 500000,
    "percentage": 150
  },
  "result": 750000
}

// 后端保存到数据库
interface FormulaData {
  type: string
  variables: Record<string, number>
  result: number
  updatedAt: Date
}
```

### 3. 数据库Schema设计

```sql
-- 添加 formula 字段到 coverages 表
ALTER TABLE coverages ADD COLUMN formula JSONB;

-- 示例数据
UPDATE coverages 
SET formula = '{
  "type": "basic_percentage",
  "variables": {"basicAmount": 500000, "percentage": 150},
  "result": 750000
}'::jsonb
WHERE id = 1;
```

---

## 常见问题

### Q1: 如何添加新的公式类型？

**A**: 在 `FormulaEditor.tsx` 中的 `FORMULA_TEMPLATES` 数组添加新模板：

```typescript
{
  type: 'my_custom_formula',
  label: '我的自定义公式',
  template: '自定义 {var1} × {var2}%',
  description: '这是一个自定义公式',
  variables: [
    {
      name: '变量1',
      key: 'var1',
      value: 100000,
      unit: '元',
      editable: true
    },
    {
      name: '变量2',
      key: 'var2',
      value: 50,
      unit: '%',
      editable: true
    }
  ]
}
```

同时更新 `calculateResult` 函数添加计算逻辑。

### Q2: 如何禁用某个变量的编辑？

**A**: 设置 `editable: false`：

```typescript
{
  name: '基本保额',
  key: 'basicAmount',
  value: 500000,
  unit: '元',
  editable: false  // 禁用编辑
}
```

### Q3: 如何自定义样式？

**A**: 覆盖 CSS 类：

```css
/* 自定义公式编辑器背景色 */
.formula-editor-card {
  background: #e6f7ff !important;
}

/* 自定义变量显示样式 */
.variable-display.editable {
  border-color: #ff4d4f !important;
}
```

### Q4: 如何验证用户输入？

**A**: 在 `onChange` 回调中添加验证逻辑：

```tsx
<FormulaEditor
  onChange={(type, variables, result) => {
    // 验证基本保额不能为0
    if (variables.basicAmount === 0) {
      message.error('基本保额不能为0')
      return
    }
    
    // 验证赔付比例在合理范围内
    if (variables.percentage < 0 || variables.percentage > 500) {
      message.error('赔付比例应在 0-500% 之间')
      return
    }
    
    // 验证通过，保存数据
    saveFormula({ type, variables, result })
  }}
/>
```

### Q5: 如何处理小数？

**A**: 组件支持小数输入，计算结果会保留完整精度。如需格式化，可以在显示时处理：

```tsx
<FormulaEditor
  onChange={(type, variables, result) => {
    const formattedResult = result.toFixed(2)  // 保留2位小数
    console.log('格式化后的结果:', formattedResult)
  }}
/>
```

---

## 技术架构

### 组件结构

```
FormulaEditor (主组件)
├── State Management (状态管理)
│   ├── formulaType (当前公式类型)
│   ├── currentTemplate (当前公式模板)
│   ├── variables (变量值)
│   └── result (计算结果)
│
├── Event Handlers (事件处理)
│   ├── handleTypeChange (切换公式类型)
│   └── handleVariableChange (更新变量值)
│
└── Sub Components (子组件)
    └── VariableInput (变量输入组件)
        ├── 显示模式 (可点击)
        └── 编辑模式 (输入框)
```

### 数据流

```
用户操作
   ↓
状态更新 (useState)
   ↓
自动计算 (useEffect)
   ↓
触发回调 (onChange)
   ↓
父组件处理
```

### 关键技术点

- **React Hooks**: `useState`, `useEffect`, `useRef`
- **TypeScript**: 严格类型检查
- **Ant Design**: UI组件库
- **CSS Modules**: 样式隔离
- **正则表达式**: 输入验证
- **千分位格式化**: 数字展示优化

---

## 更新日志

### v1.0 (2026-01-09)
- ✅ 初始版本发布
- ✅ 支持7种常见公式类型
- ✅ 两层交互设计
- ✅ 实时计算和格式化
- ✅ 完整的TypeScript类型定义
- ✅ 响应式布局支持

---

## 联系方式

如有问题或建议，请联系开发团队。

**文档结束**

